apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{pvc_name}}
  namespace: {{ns}}
  annotations:
    volume.beta.kubernetes.io/storage-class: nfs-policy
  labels:
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  storageClassName: nfs-policy
